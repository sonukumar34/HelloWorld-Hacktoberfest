SET SERVEROUTPUT ON

DECLARE
   X   VARCHAR2 (50);
   MAYUS VARCHAR2(100);
   FECHA DATE;

BEGIN
   X := 'hello world'; 
   MAYUS:=UPPER(X);
   DBMS_OUTPUT.PUT_LINE (MAYUS);
   
   FECHA:=SYSDATE;
   DBMS_OUTPUT.PUT_LINE (FECHA);
END;
/
